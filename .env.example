# Copy this file to .env.local for local development
# Add real values only in .env.local / hosting provider env settings

# Example public variable (safe to expose to browser if needed)
# NEXT_PUBLIC_SITE_URL=https://example.com

# Example server-only variable (keep secret)
# API_SECRET_KEY=your_secret_here