import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        "neon-green": "#54EB17",
        "taest-pink": "#FF2782",
        "dark-grey": "#2E2E2E",
      },
    },
  },
};

export default config;
